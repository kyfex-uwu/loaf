# loaf

let others loaf on your keyboard

note: this currently only works on linux distros that use X11

installation:
- clone the repository
- run npm init
- rename `example.env` to `.env` and fill it out
- run `npm run-script run`
